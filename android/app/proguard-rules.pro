# GeckoView ships its own consumer rules; keep the bridge surface minimal.
# No rules are required beyond the defaults, but we keep this file so that
# any future keep-rules are documented in one place.
